In this folder the optimization outcome form the virtual machine is moved (using WinSCP).
This includes the data indicateb by: "solution_" and "solutionAlldata"
Data is not tracked by the git repository --> see gitignore.

Tool also provides the following files, which we might not use:
- objectiveValues
- objectiveRanges (csv/json)
