In this folder the optimization outcome form the virtual machine is moved (using WinSCP).
This includes the data indicateb by: "solution_" and "solution_alldata"
Data is not tracked by the git repository --> see gitignore.

